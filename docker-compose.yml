services:
  # --------------------------------------------------------
  # Banco de Dados: PostgreSQL 16 (Puro)
  # --------------------------------------------------------
  zabbix-db:
    image: postgres:16-alpine
    container_name: zabbix-db
    restart: always
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-zabbix}
      POSTGRES_USER: ${POSTGRES_USER:-zabbix}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-SuaSenhaSeguraAqui123!}
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - backend-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-zabbix} -d ${POSTGRES_DB:-zabbix}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # --------------------------------------------------------
  # Zabbix Server 7.0 LTS (Postgres)
  # --------------------------------------------------------
  zabbix-server:
    image: zabbix/zabbix-server-pgsql:alpine-7.0-latest
    container_name: zabbix-server
    restart: always
    ports:
      - "${ZABBIX_SERVER_PORT:-10051}:10051"
    environment:
      DB_SERVER_HOST: zabbix-db
      POSTGRES_DB: ${POSTGRES_DB:-zabbix}
      POSTGRES_USER: ${POSTGRES_USER:-zabbix}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-SuaSenhaSeguraAqui123!}
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - zabbix-alertscripts:/usr/lib/zabbix/alertscripts
      - zabbix-externalscripts:/usr/lib/zabbix/externalscripts
    networks:
      - backend-net
      - frontend-net
    depends_on:
      zabbix-db:
        condition: service_healthy

  # --------------------------------------------------------
  # Zabbix Web Interface (Nginx + PHP)
  # --------------------------------------------------------
  zabbix-web:
    image: zabbix/zabbix-web-nginx-pgsql:alpine-7.0-latest
    container_name: zabbix-web
    restart: always
    ports:
      - "${ZABBIX_WEB_PORT:-8080}:8080"
    environment:
      DB_SERVER_HOST: zabbix-db
      POSTGRES_DB: ${POSTGRES_DB:-zabbix}
      POSTGRES_USER: ${POSTGRES_USER:-zabbix}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-SuaSenhaSeguraAqui123!}
      ZBX_SERVER_HOST: zabbix-server
      PHP_TZ: "America/Sao_Paulo"
    volumes:
      - /etc/localtime:/etc/localtime:ro
    networks:
      - backend-net
      - frontend-net
    depends_on:
      - zabbix-db
      - zabbix-server

networks:
  backend-net:
    driver: bridge
    internal: true # Isolamento total do banco de dados na rede interna
  frontend-net:
    driver: bridge

volumes:
  pgdata:
  zabbix-alertscripts:
  zabbix-externalscripts:
